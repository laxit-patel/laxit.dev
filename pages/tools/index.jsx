import Color from '../../components/utils/page.colors.util'
import colors from '../../content/resume/_colors.json'
import List from '../../components/sections/tools/list'

export default function CaseStudies({ }) {
	return (
		<>
			<Color colors={colors} />
			<List />
		</>
	)
}